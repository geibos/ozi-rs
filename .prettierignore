node_modules/
build/
.svelte-kit/
dist/
src-tauri/target/
.sisyphus/
package-lock.json

# Generated by tauri-specta (`cargo test typescript_bindings_are_up_to_date`).
# Formatting it makes that test fail on the next run, which cost a confusing
# minute every time the frontend was formatted.
src/lib/bindings.ts

# Written by the Rust core (`just fixtures`); same reason.
src/test/fixtures/*.json
