---
description: Implement one agreed backlog item with tests and docs
agent: coder
---

Read `AGENTS.md` and relevant docs first.

Implement this backlog item:

$ARGUMENTS

Before changing files:
- summarize the goal;
- list affected modules/files;
- list tests to add or update.

Then implement the smallest coherent version of the task.

After implementation:
- run relevant checks;
- update docs if needed;
- summarize changes, tests, and remaining risks.
