{
  "$schema": "../node_modules/@tauri-apps/cli/schema/acl-manifest.json",
  "identifier": "default",
  "description": "Default permissions for all windows",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-destroy",
    "dialog:allow-confirm",
    "dialog:allow-open",
    "dialog:allow-save",
    "shell:allow-open"
  ]
}
