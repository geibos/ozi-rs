# Feature Status

This table separates backend capability, UI exposure, documentation state, implementation
status, and evidence. Use it to avoid broad “supported” claims when a feature is only
partially available.

| Feature | Backend | UI | Docs | Status | Evidence | Audit-verified |
|---------|---------|----|------|--------|----------|---|
| Startup restore | Infrastructure session persistence stores the last project path and active map reference/path separately from `.ozp` files. Missing referenced files degrade to fresh state or restored project with a warning instead of panic. | Startup uses the restored project/map only. No viewport, panel, selected entity, undo, theme, unsaved edit, or bundle-loader window state is restored by the Rust session file. | README, roadmap, architecture, ADR-0019, and `docs/persistence-session.md` describe bounded restore scope. | Implemented, bounded | Task 2 session restore tests and notepad evidence. | TBD (smoke pending) |
| Track point timestamp display | Imported/project track points expose optional timestamp values through track-detail DTOs. | `TrackPointsPanel.svelte` renders timestamp text when present and omits placeholder text when absent. Sorting by timestamp remains planned/not implemented. | Requirements and roadmap distinguish timestamp display from deferred timestamp sorting. | UI surfaces existing data | Task 3 `src/test/track-points-panel.test.ts` source-level guard. | TBD (smoke pending) |
| Track color controls | Backend supports per-track color mutation and GeoJSON rendering uses color properties. | `TracksPanel.svelte` surfaces a color picker that calls the typed `setTrackColor()` wrapper. | README, requirements, frontend architecture, and this matrix list color as surfaced UI. | Implemented UI control | Task 4 `src/test/tracks-panel-style.test.ts`; no direct component `invoke()` guard. | pending ([smoke-track-style.md](smoke-track-style.md)) |
| Track line width controls | Backend supports per-track line-width mutation and GeoJSON rendering uses width properties. | `TracksPanel.svelte` surfaces a 1-12 px line-width control that calls the typed `setTrackLineWidth()` wrapper. | README, requirements, frontend architecture, and this matrix list line width as surfaced UI. | Implemented UI control | Task 4 `src/test/tracks-panel-style.test.ts`. | pending ([smoke-track-style.md](smoke-track-style.md)) |
| Waypoint symbol undo | `ProjectCommand::SetWaypointSymbol` routes symbol changes through the command stack with old/new symbols for reverse deltas. | Existing symbol picker commits choices through `setWaypointSymbol()`; undo/redo applies to each committed symbol choice. | Architecture and commands reference list `SetWaypointSymbol`/`set_waypoint_symbol` as undoable. | Undoable project edit | Task 5 `waypoint_symbol_undo` and `waypoint_symbol_missing` Rust tests. | TBD (smoke pending) |
| OK-standard warning | Backend remains permissive; it does not reject non-conforming names. | `TracksPanel.svelte` surfaces warning-only validation for `^\d{8}_.*\S.*$`, allowing Cyrillic callsigns and not validating calendar dates. Rename/save/export are not blocked. | README, requirements, roadmap, ADR-0019, and this matrix use warning-only language. | UI warning implemented | Task 6 `src/test/track-name-validation.test.ts`. | pending ([smoke-ok-standard.md](smoke-ok-standard.md)) |
| 10-Tracks export default | Backend capability `get_track_export_default_path` builds active-bundle `10-Tracks/<track>.<ext>` suggestions and returns no suggestion when no active bundle can be inferred. | GPX and PLT export dialogs use the backend suggestion when present and keep filename-only fallback otherwise. Users can choose another path. | README, requirements, roadmap, commands reference, ADR-0019, and this matrix describe suggested defaults, not forced export. | Implemented suggestion | Task 7 `export_default_tracks_dir` Rust test and `src/test/export-default-path.test.ts`. | TBD (smoke pending) |
| Multi-layer workflows | Backend supports multiple track and waypoint layers and `AppStateDto` exposes layer summaries. | UI surfaces active-layer selection for existing drawing, waypoint add/move/list, and track-detail workflows. Full layer management (create/rename/delete/reorder UI) is not implemented. | README, requirements, roadmap, frontend architecture, ADR-0019, and this matrix say active-layer selection rather than full layer management. | Minimal UI implemented; full manager planned/not implemented | Task 8 `src/test/active-layer-ui.test.ts` and no-hardcoded-workflow guard. | TBD (smoke pending) |
| `windows.ts` bundle-loader lifecycle | Tauri secondary window support exists through a `bundles` webview at `/?view=bundles`. | `src/main.ts` pre-creates the hidden loader on main-window startup; `Sidebar.svelte` opens it through `openBundleLoader()`, which reuses, shows, and focuses the existing window or creates a fallback. Session restore does not persist window open/closed state. | `docs/frontend-architecture.md` documents `src/lib/windows.ts`, `precreateBundleLoader()`, `openBundleLoader()`, and the `view=bundles` route; `docs/persistence-session.md` states window open state is not restored. | Documented current behavior | Task 9 required-term check and window-session grep. | TBD (smoke pending) |
| Backend capability/status split | Backend capability varies by feature: some behaviors are fully implemented, some are backend-only, and some remain planned/not implemented. | UI exposure is listed separately so docs do not equate backend support with surfaced workflows. | This table is the public status split for audit findings and uses “backend supports,” “UI surfaces,” “planned,” and “not implemented” where appropriate. | Complete for the 10 audit findings | Task 10 feature-status coverage and unsupported-claims Python checks. | TBD (smoke pending) |
| Bundle open — local folder | Backend support: `load_projects` enumerates local `.ozp` files and creates project references. Active map per-project is persisted separately. | File picker accessible via `Maps…` button in sidebar; selected bundle loads and displays active map via GeoJSON tile pipeline. | README, ADR-0020 section Maps, and `docs/persistence-session.md` document bundle loading and project enumeration. | MVP, implemented | Task 3 `smoke-bundle-open.md` — AX-tree verification of active map and project list. | partial ([smoke-bundle-open.md](smoke-bundle-open.md)) |
| Bundle open — URL (LizaAlert) | Backend support: URL loading via `load_projects` would resolve LizaAlert live project feed. | UI: Maps… → URL input not present in current MVP. | ADR-0020 describes LizaAlert integration as future scope, not MVP. | MVP, not implemented | Task 3 fixture missing; integration pending external LizaAlert feed. | hidden ([smoke-bundle-open.md](smoke-bundle-open.md)) |
| Map switch (Topo ↔ Satellite, MBTiles + OZF2) | Backend: `tile_source_changed` event routes map switches through rendering pipeline; OZF2 and MBTiles tile loaders both operational. | UI: Map Bundles secondary window lists available maps; click switches active map (tested via AX tree). | README, ADR-0020 section Maps, architecture docs describe tile pipeline and source switching. | MVP, implemented | Task 4 `smoke-map-switch.md` — OZF2 Satellite verified active; click not yet driven (F8 blocker). | partial ([smoke-map-switch.md](smoke-map-switch.md)) |
| MBTiles Topo tile loading | Backend: `sqlite://` tile scheme loads MBTiles database and feeds tiles to MapLibre. | UI: Maps… button opens secondary window; bundle list shows available maps (structure partially unclear in AX tree). | README, ADR-0020 describe MBTiles as first-class tile source. | MVP, implemented | Task 4 `smoke-mbtiles-tiles.md` — Maps window open; bundle list AX-tree structure unclear; click attempt blocked by F8. | partial ([smoke-mbtiles-tiles.md](smoke-mbtiles-tiles.md)) |
| OZF2 raster tile loading | Backend: `ozi://` tile scheme loads OZF2 raster maps and feeds tiles to MapLibre. | UI: Maps… button opens secondary window; OZF2 maps listed and selectable. | README, ADR-0020 describe OZF2 as legacy tile source with full support. | MVP, implemented | Task 4 `smoke-ozf2-tiles.md` — OZF2 Satellite confirmed rendering on startup; bundle list navigation unclear. | partial ([smoke-ozf2-tiles.md](smoke-ozf2-tiles.md)) |
| OSM online fallback | Backend: MapLibre configured with OpenStreetMap as fallback when no local bundle active. | UI: No “Unload Bundle” or “Disable Maps” button visible to trigger fallback. OSM attribution shows when map renders. | ADR-0020 describes fallback as fallback rendering, not user-triggered. | MVP, partially implemented | Task 4 `smoke-osm-fallback.md` — OSM attribution visible; no UI to explicitly trigger fallback. | hidden ([smoke-osm-fallback.md](smoke-osm-fallback.md)) |
| GPX track import | Backend: `import_track_gpx` handler accepts GPX file, parses points, creates track with geometry. | UI: “Import GPX” button in sidebar opens file picker; imported track renders on map and appears in Tracks panel. | README, ADR-0020 describe GPX import as core track workflow. | MVP, implemented | Task 5a `smoke-track-import-gpx.md` — GPX button confirmed in AX tree; import flow not yet driven (click-dependent). | pending ([smoke-track-import-gpx.md](smoke-track-import-gpx.md)) |
| PLT track import | Backend: `import_track_plt` handler accepts OziExplorer PLT format, parses points, creates track. | UI: “Import PLT” button in sidebar opens file picker; imported track renders and appears in Tracks panel. | README, ADR-0020 describe PLT import as OziExplorer compatibility feature. | MVP, implemented | Task 5b `smoke-track-import-plt.md` — PLT button confirmed in AX tree; import flow pending Appium run. | pending ([smoke-track-import-plt.md](smoke-track-import-plt.md)) |
| ZIP archive track import | Backend: File picker accepts `.zip` files; handler detects multiple track files and imports each. | UI: File picker during GPX/PLT import includes `.zip` in file types; no separate ZIP import button. | ADR-0020 describes ZIP as enhancement to basic import flow. | MVP, enhancement planned | Task 5c `smoke-track-import-zip.md` — ZIP support not yet confirmed; fixture will be created if backend supports it. | hidden ([smoke-track-import-zip.md](smoke-track-import-zip.md)) |
| Multi-track display + visibility toggle | Backend: `AppStateDto` exposes all tracks; visibility state per track. | UI: Tracks panel lists imported tracks; eye icon toggles visibility; polylines render/hide on map accordingly. | README, ADR-0020 describe multi-track support as core feature. | MVP, implemented | Task 5d `smoke-track-display.md` — Multiple imports possible; visibility toggle UI expected in Tracks panel. | pending ([smoke-track-display.md](smoke-track-display.md)) |
| Track color and line-width styling | Backend: `set_track_color`, `set_track_line_width` commands mutate style; GeoJSON renderer respects color/width properties. | UI: Tracks panel or context menu exposes color picker and width slider for each track. | README, ADR-0020 describe track styling as core feature. | MVP, implemented | Task 5e `smoke-track-style.md` — Style controls expected in panel or context menu; discovery and interaction pending. | pending ([smoke-track-style.md](smoke-track-style.md)) |
| Large-track load performance (>10k points) | Backend: Track load and rendering must complete in <2 s for 10k+ point tracks. | UI: No performance indicator; successful load implies success. | ADR-0020 implies performance target for large mission data. | MVP, not tested | Task 5f `smoke-track-large.md` — No >10k fixture available; performance verification blocked. | hidden ([smoke-track-large.md](smoke-track-large.md)) |
| Track points panel discovery | Backend: `get_track_detail` returns point list with lat/lon/timestamp/elevation. | UI: User reports inability to find points panel. Discovery required: is panel button present? Accessible via double-click or context menu? | ADR-0020 describes track points detail as core feature. | MVP, likely implemented but unreachable | Task 6a `smoke-track-points-panel.md` — Discovery goal: find entry point (button, context menu, double-click). | pending ([smoke-track-points-panel.md](smoke-track-points-panel.md)) |
| Track point walkthrough (next/previous) | Backend: Points stored with index; next/previous navigation by index. | UI: Buttons or keyboard nav in points panel; selected point updates info panel; visual highlight on map. | ADR-0020 describes point navigation as core task workflow. | MVP, likely implemented | Task 6b `smoke-track-walkthrough.md` — Buttons/keyboard nav expected in open points panel. | pending ([smoke-track-walkthrough.md](smoke-track-walkthrough.md)) |
| Track point delete | Backend: `delete_track_point` command removes point and triggers undo/redo. | UI: Right-click or Delete key on point in panel or on map vertex; point removed from panel and polyline updates. | ADR-0020, ADR-0019 (undo) describe point deletion as essential editing operation. | MVP, implemented | Task 6c `smoke-track-point-delete.md` — Delete via context menu or keyboard on point. | pending ([smoke-track-point-delete.md](smoke-track-point-delete.md)) |
| Track segment break (split) | Backend: `break_track_segment` command inserts gap at point; polyline shows two segments. | UI: Right-click on point in panel or vertex on map → “Segment Break” menu item; polyline updates. | ADR-0020 describes segment break as SAR-specific editing feature (marks gaps). | MVP, implemented | Task 6d `smoke-track-segment-break.md` — Break action accessible via context menu or dialog. | pending ([smoke-track-segment-break.md](smoke-track-segment-break.md)) |
| Sort track points by timestamp | Backend: `sort_track_points` command reorders points in ascending timestamp order. | UI: Button or menu item labeled “Sort by Timestamp” in points panel or track context menu; points reorder. | ADR-0020 section Tracks marks this as MVP-must feature. | MVP, must implement | Task 6e `smoke-track-sort-by-time.md` — UI entry point discovery critical; missing = `hidden` → MVP gap. | pending ([smoke-track-sort-by-time.md](smoke-track-sort-by-time.md)) |
| Douglas–Peucker simplification | Backend: `simplify_track` command with tolerance parameter reduces point count via DP algorithm. | UI: Dialog or panel with tolerance slider; optional preview on map; Apply button commits change. | ADR-0020 describes simplification as track editing feature. | MVP, enhancement planned | Task 6f `smoke-track-simplify.md` — Simplify button/menu and tolerance slider expected. | pending ([smoke-track-simplify.md](smoke-track-simplify.md)) |
| Crop track (by extent, time, selection) | Backend: Three separate commands: `crop_to_extent`, `crop_by_time_range`, `crop_by_selection`. | UI: Track context menu or points panel with multiple crop mode buttons/menu items. | ADR-0020 describes three crop modes for mission data cleanup. | MVP, enhancement planned | Task 6g `smoke-track-crop.md` — Three sub-tests; success if >=1 mode works. | pending ([smoke-track-crop.md](smoke-track-crop.md)) |
| GPX track export | Backend: `export_track_gpx` command writes track to GPX file with 10-Tracks/ default path. | UI: Right-click track in panel → “Export as GPX”; file picker opens; exported file valid GPX with correct point count. | README, ADR-0020 describe GPX export as core output workflow. | MVP, implemented | Task 6h `smoke-track-export-gpx.md` — Export menu/button accessible; file creation and format validation. | pending ([smoke-track-export-gpx.md](smoke-track-export-gpx.md)) |
| PLT track export | Backend: `export_track_plt` command writes track to OziExplorer PLT format. | UI: Right-click track → “Export as PLT”; file picker opens; exported file valid PLT. | README, ADR-0020 describe PLT export for OziExplorer compatibility. | MVP, implemented | Task 6i `smoke-track-export-plt.md` — Export menu/button accessible; format validation. | pending ([smoke-track-export-plt.md](smoke-track-export-plt.md)) |
| Track drawing | Backend: `create_track` with points added via `add_track_point` during drawing session. | UI: Drawing mode toggle (button or sidebar state); map clicks create waypoints; polyline connects them; new track in Tracks panel. | ADR-0020 describes track drawing as core creation workflow. | MVP, implemented | Task 6j `smoke-track-draw.md` — Drawing mode toggle and map-click interaction. | pending ([smoke-track-draw.md](smoke-track-draw.md)) |
| Waypoint add | Backend: `create_waypoint` command with map coordinates. | UI: “Add Waypoint” button in sidebar opens waypoint dialog; map click creates marker; panel row appears. | ADR-0020 describes waypoint addition as core workflow. | MVP, implemented | Task 7a `smoke-waypoint-add.md` — Add button/dialog discovery; marker and panel row creation. | pending ([smoke-waypoint-add.md](smoke-waypoint-add.md)) |
| Waypoint move | Backend: `set_waypoint_position` command updates coordinates; undo coalesces drag events. | UI: Drag waypoint marker on map; coordinates update in panel; drag events coalesced into single undo step. | ADR-0020 describes waypoint movement as core workflow. | MVP, implemented | Task 7b `smoke-waypoint-move.md` — Drag interaction and undo coalescing. | pending ([smoke-waypoint-move.md](smoke-waypoint-move.md)) |
| Waypoint delete | Backend: `delete_waypoint` command removes waypoint; undo restores it. | UI: Right-click or Delete key on waypoint in panel or on map; waypoint removed; undo restores. | ADR-0020, ADR-0019 describe waypoint deletion as essential editing operation. | MVP, implemented | Task 7d `smoke-waypoint-delete.md` — Delete via context menu or keyboard; undo verification. | pending ([smoke-waypoint-delete.md](smoke-waypoint-delete.md)) |
| Waypoint rename | Backend: `set_waypoint_name` command updates name; undo/redo supported. | UI: Double-click or edit button on waypoint name in panel; text editable; confirm persists; labels update on map. | ADR-0020 describes waypoint renaming as core editing feature. | MVP, likely implemented | Task 7c `smoke-waypoint-rename.md` — Name edit via double-click or button; map label update. | pending ([smoke-waypoint-rename.md](smoke-waypoint-rename.md)) |
| Waypoint color and symbol customization | Backend: `set_waypoint_color`, `set_waypoint_symbol` commands; rendering respects color/symbol. | UI: Color picker and symbol picker accessible from waypoint row or context menu; changes visible on map and panel; persistence confirmed. | ADR-0020 describes waypoint styling as enhancement feature. | MVP, enhancement planned | Task 7e `smoke-waypoint-style.md` — Color picker and symbol picker discovery and interaction. | pending ([smoke-waypoint-style.md](smoke-waypoint-style.md)) |
| Waypoint multi-display + visibility toggle | Backend: `AppStateDto` exposes all waypoints; visibility state per waypoint. | UI: Waypoints panel lists all waypoints; individual and/or panel-level eye icon toggles visibility; hidden waypoints don't render on map. | ADR-0020 describes multi-waypoint support with visibility toggle. | MVP, implemented | Task 7f `smoke-waypoint-multi.md` — Multi-waypoint rendering and visibility toggle. | pending ([smoke-waypoint-multi.md](smoke-waypoint-multi.md)) |
| Waypoint export GPX | Backend: `export_waypoints_gpx` command writes waypoints to GPX file. | UI: Right-click waypoint or panel menu → “Export as GPX”; file picker opens; exported file valid GPX with correct waypoint count. | ADR-0020 describes GPX export as core output workflow. | MVP, implemented | Task 7g `smoke-waypoint-export-gpx.md` — Export menu/button and file creation. | pending ([smoke-waypoint-export-gpx.md](smoke-waypoint-export-gpx.md)) |
| Waypoint export PLT | Backend: `export_waypoints_plt` command writes waypoints to OziExplorer PLT format. | UI: Right-click waypoint or panel menu → “Export as PLT”; file picker opens; exported file valid PLT. | ADR-0020 describes PLT export for OziExplorer compatibility. | MVP, implemented | Task 7h `smoke-waypoint-export-plt.md` — Export menu/button and format validation. | pending ([smoke-waypoint-export-plt.md](smoke-waypoint-export-plt.md)) |
| Waypoint export WPT | Backend: `export_waypoints_wpt` command writes waypoints to native OziExplorer WPT format. | UI: Right-click waypoint or panel menu → “Export as WPT”; file picker opens; exported file valid WPT. | ADR-0020, ADR-0022 mark WPT export as MVP-critical for SAR field compatibility. | MVP, must implement | Task 7i `smoke-waypoint-export-wpt.md` — CRITICAL: If UI missing, flag as MVP blocker (ADR-0022). | pending ([smoke-waypoint-export-wpt.md](smoke-waypoint-export-wpt.md)) |
| On-map distance measurement tool | Backend: Measurement calculation from two points. | UI: Button/menu to activate tool; user clicks two points on map; distance displays (km/mi). | ADR-0020 section On-map Tools lists distance as MVP-must feature. | MVP, not implemented | Task 8a `smoke-tool-distance.md` — No distance button/panel found in AX tree or toolbar. | missing ([smoke-tool-distance.md](smoke-tool-distance.md)) |
| On-map circle with explicit radius | Backend: Circle rendering at specified center/radius (meters). | UI: Button to activate tool; radius input (dialog or slider); circle renders on map. | ADR-0020 section On-map Tools lists circle as MVP-must feature. | MVP, not implemented | Task 8b `smoke-tool-circle.md` — No circle button/panel found in toolbar or sidebar. | missing ([smoke-tool-circle.md](smoke-tool-circle.md)) |
| Waypoint projection tool | Backend: Projection calculation from point, bearing, distance. | UI: Right-click waypoint or map → “Project Waypoint”; bearing/distance input; new waypoint created at projected location. | ADR-0020 section On-map Tools lists projection as MVP-must feature. | MVP, not implemented | Task 8c `smoke-tool-projection.md` — No projection menu option or button found. | missing ([smoke-tool-projection.md](smoke-tool-projection.md)) |
| Save/Load project (.ozp) | Backend: `save_project` serializes app state to .ozp file; `load_project` deserializes and restores state (tracks, waypoints, styles, visibility, zoom, center). | UI: Cmd+S or File menu → Save opens file picker; project file created; Cmd+O or File menu → Open loads project identically. | README, ADR-0019 describe project persistence as core feature. | MVP, implemented | Task 9a `smoke-project-save-load.md` — Save/load flow with full state restoration. | pending ([smoke-project-save-load.md](smoke-project-save-load.md)) |
| Recent projects list | Backend: Session file tracks recently opened projects in reverse chronological order. | UI: File → Open Recent or sidebar button lists ≥3 recent projects; click opens correct project. | ADR-0020 marks Recent Projects as MVP-must feature. | MVP, implementation uncertain | Task 9b `smoke-project-recent.md` — High probability of `missing` (only last-project auto-restore currently documented). | pending ([smoke-project-recent.md](smoke-project-recent.md)) |
| Undo/Redo | Backend: `AppStateDto` maintains command stack; `undo`, `redo` commands execute reverse and forward deltas. | UI: Cmd+Z (undo) and Cmd+Shift+Z (redo) or Edit menu buttons; each action undoable as single step (drag events coalesced). | README, ADR-0019 describe undo/redo as essential usability feature. | MVP, implemented | Task 9c `smoke-undo-redo.md` — Verify Cmd+Z, Cmd+Shift+Z for waypoint move, delete, track point delete. | pending ([smoke-undo-redo.md](smoke-undo-redo.md)) |
| OK-standard track name validation | Backend: Permissive; names not rejected. | UI: `TracksPanel.svelte` shows ⚠️ warning icon for names not matching `^\d{8}_.*\S.*$`; warning disappears on valid name. | README, ADR-0019 describe warning-only validation for YYYYMMDD_Callsign format. | MVP, warning implemented | Task 9d `smoke-ok-standard.md` — Track name validation visual indicator and format specification. | pending ([smoke-ok-standard.md](smoke-ok-standard.md)) |
| Theme selection (Catppuccin) | Backend: Theme CSS applied on startup and persisted in session file. | UI: PROJECT sidebar “Auto” PopUpButton (or Settings) with 5 theme options (Latte, Frappé, Macchiato, Mocha, Auto); selection applies and persists. | ADR-0020 describes Catppuccin theme set as UI feature. | MVP, enhancement planned | Task 9e `smoke-themes.md` — Theme picker discovery and visual application. High probability of `missing`. | pending ([smoke-themes.md](smoke-themes.md)) |
| Dev console + FPS counter | Backend: Console logs exposed via browser dev tools or in-app terminal; FPS calculated from frame times. | UI: Backtick (`) key toggles console; F3 key toggles FPS counter in corner; both toggleable independently. | ADR-0020 describes dev tools as debugging feature. | MVP, enhancement planned | Task 9f `smoke-devtools.md` — Console and FPS counter toggle via keyboard shortcut. | pending ([smoke-devtools.md](smoke-devtools.md)) |
