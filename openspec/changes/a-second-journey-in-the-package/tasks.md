## 1. The journey

- [x] 1.1 `smoke_layer_management`: menu → new layer → create → active → delete
- [x] 1.2 Clicks only — the name arrives filled in, and typing through Mac2 has never worked
- [x] 1.3 Its labels added to the smoke-label contract

## 2. What it found

- [x] 2.1 `just smoke` runs one journey at a time; two sessions fought over the screen
- [x] 2.2 The layer select's accessible name carries the active layer
- [x] 2.3 The composition moved into the script, so the untranslated-label guard can read it

## 3. Gates

- [x] 3.1 `just ci` green
- [x] 3.2 `just smoke` green — both journeys, against a bundle built the same hour
