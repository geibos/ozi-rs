## 1. Backend

- [x] 1.1 A round-trip test covering name, segments, coordinates, elevation, timestamps, colour and width

## 2. Verification

- [x] 2.1 `just ci` green — the test passes as written, which is the finding
- [ ] 2.2 `just smoke` green (blocked: the Mac2 driver cannot enable automation mode — see `docs/STATE.md`)
