## 1. Backend

- [x] 1.1 Parse the size cell of each listing row, in both alphabets, with tests
- [x] 1.2 `size_bytes` on `LizaMapPackage`, filled from the listing and from disk for cached maps
- [x] 1.3 `size_bytes` on the DTO; bindings regenerated

## 2. Frontend

- [x] 2.1 One shared `formatBytes`, locale-aware, replacing the three copies, with tests
- [x] 2.2 Size on each map row in the loader and the Maps tab
- [x] 2.3 Translate the cold-start status bar's remaining English

## 3. Verification

- [x] 3.1 `just ci` green
- [ ] 3.2 Screenshot in `docs/progress/`
