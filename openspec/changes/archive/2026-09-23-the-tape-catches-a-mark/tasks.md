# Tasks

- [x] 1.1 `src/lib/measure-snap.ts`: the nearest mark within a screen radius,
      or the click; and the naming of a two-ended measurement.
- [x] 1.2 Ten tests written first, including the two that say what the reach
      means: the same click catches at one zoom and not at another, and two
      marks at the same place do not make the answer depend on draw order.
- [x] 1.3 The map's measure click snaps; the readout names the pair.
- [x] 1.4 Walked on the stand: clicking eight pixels off each of two marks
      reads `60 м · ШТАБ → ЗАБРОС`.
- [x] 1.5 `just ci` green.
