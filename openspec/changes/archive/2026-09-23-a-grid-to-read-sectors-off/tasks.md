# Tasks

- [x] 1.1 `src/lib/graticule.ts`: the step ladder, the choice of step, the
      lines, and the notation — the parts that are decisions about numbers.
- [x] 1.2 Seventeen tests written first, including the two things that go wrong
      quietly: `59°59'60"` where a whole degree was meant, and a line whose
      name drifts away from it after forty accumulated steps of one second.
- [x] 1.3 `src/lib/maplibre/graticule-layer.ts`: a GeoJSON line layer beneath
      the work, DOM markers for the names (the style bundles no glyphs), and a
      handle that takes the whole thing off again.
- [x] 1.4 A store remembered per machine, and an entry in the command palette.
- [x] 1.5 Walked on the stand at four zooms: 2″ at a fifty-metre view, 10″ at
      three hundred metres, 1′ at two kilometres, 5′ at ten — between ten and
      fourteen labels throughout.
- [x] 1.6 `just ci` green.
