## 1. Backend

- [x] 1.1 The importers report whether the file declared a colour, with tests written first
- [x] 1.2 `TRACK_PALETTE` and assignment by position among the project's tracks
- [x] 1.3 A declared colour is kept, from GPX and from PLT

## 2. Stand

- [x] 2.1 The import answers carry the same palette

## 3. Verification

- [x] 3.1 On the stand, four folder imports: fifteen rows, thirteen distinct swatches — the palette's twelve, wrapping, plus the fixture's own
- [x] 3.2 `just ci` green (338 Rust, 500 frontend)
- [ ] 3.3 `just smoke` green (blocked: the Mac2 driver cannot enable automation mode — see `docs/STATE.md`)
