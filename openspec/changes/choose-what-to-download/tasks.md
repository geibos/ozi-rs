## 1. Backend

- [x] 1.1 `skip_top_level` through the config and the recursive walk, skipping only at the top level — the granularity the listing shows
- [x] 1.2 `BundleEntry` on `LizaProject`, from the listing online and from the directory offline
- [x] 1.3 `load_project` takes the skip list; DTO and bindings

## 2. Frontend

- [x] 2.1 The checklist, everything checked, resetting when another project is previewed
- [x] 2.2 Sizes where the listing gives them, "папка" otherwise, and a hint naming what is usually heavy

## 3. Verification

- [x] 3.1 Rust test: a named entry is left on the server, the rest arrives
- [x] 3.2 `just ci` green
- [x] 3.3 Seen on the stand
