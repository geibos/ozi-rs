## 1. Frontend

- [x] 1.1 The extension lives in one module, with why Open still takes `json`
- [x] 1.2 Save offers `.ozp`; Open offers `ozp` then `json`
- [x] 1.3 Tests: what Save offers, what Open accepts, and that a cancelled dialog saves nothing
- [x] 1.4 The CJ-7 assertion that pinned `extensions: ["json"]` replaced — it pinned the defect, not the contract

## 2. Gates

- [x] 2.1 `just ci` green
- [ ] 2.2 `just smoke` green (blocked: the Mac2 driver host crashes at session creation — see `docs/STATE.md`)
